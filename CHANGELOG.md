# 更新记录

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## 0.2.0

**新增**

- **设置集成进 harness**：DSH 的「设置 → 邮件通知」里可以改全部配置（SMTP 服务器/端口/账号/授权码/收件人、
  三种触发开关、界面提示开关、邮件内容、限流参数），带「保存设置」「发送测试邮件」「重新读取」三个按钮，
  还会显示当前状态（配置是否就绪、你此刻是否被判定为"在看界面"、上次发信结果）。
  装好后不再需要命令行或手改 `config.json`。
- **新增「助手向我提问」通知**：工具 `ask_user_question` 被调用（任务停在等你回答）时，离开电脑就发邮件。
  邮件里带上问题原文与选项。
- `GET/POST /dsh-email-notify/config` 接口：读配置不回显授权码（只报"已设置"），写配置时授权码留空表示不修改。
- `POST /dsh-email-notify/test` 支持传入界面上的**草稿**配置，所以"还没保存也能先试发一封"。
- `CHANGELOG.md`、`.gitignore`、仓库元数据（作者/仓库/issue 地址），可直接发布到 npm 或 GitHub。

**修复**

- **邮件主题缺任务标题**：插件通常在会话已经开始之后才挂载（或 DSH 重启过），内存里没有标题事件。
  现在会回读会话日志里的 `session/title`，主题与正文都能带上任务标题。
- **界面提示文案为空**：宿主端推给客户端的字段名（`title`/`outcome`）和客户端渲染用的
  （`notifyTitle`/`notifyBody`）对不上，会导致"看界面时弹提示"弹出空白卡片。现已统一，并由测试守住这个契约。
- 测试邮件的结果会记进 `/status` 的 `lastSend`（此前发过测试邮件后仍显示 `null`，容易让人误以为没发出去）。

**变更**

- `notifyWhenFocused` 默认改为 `false`：桌面外壳自己会在看界面时弹"任务完成"系统通知
  （它的误报已在 `main.js` 里修掉），插件不再重复弹一条。想由插件接管就打开这个开关。
- 配置文件模板改为**空值**（此前写的是"你的QQ邮箱@qq.com"之类的占位文字，显示在设置界面里会被误当成真实值）。
- 安装脚本不再在命令行里索要邮箱与授权码，只负责装插件；配置一律在设置界面里填。
- `smtp.allowInsecure`（允许无 TLS 明文发送）从"隐式默认"变成正式配置项，设置界面里可勾选，默认关闭。
- 设置面板保存时**只回传改动过的字段**（不再把整份配置写回去），这样以后升级插件改默认值仍然生效，
  也不会把你没碰过的默认项固化进 `config.json`。
- 新增 `scripts/update-all.mjs`（双击 `更新并修复.cmd`）：一次做完"同步插件 + 打外壳补丁"，
  顺序固定、可 `--dry` 预演；DSH 还在运行时会安全跳过外壳补丁并提示稍后补跑。

## 0.1.0

首个版本。

- 宿主端订阅会话事实：`turn/end`（任务完成/出错/中止）、`approval/asked`（工具请求授权）。
- 由浏览器半侧上报窗口焦点与可见性判定"是否在看界面"：在看就不发邮件，离开才发。
- 零依赖 SMTP 客户端：465 隐式 TLS / 587 STARTTLS、AUTH LOGIN·PLAIN、RFC 2047 中文主题、base64 正文，
  失败时给中文可读提示；默认拒绝没有 TLS 的明文服务器以保护授权码。
- 提供 `presence` / `inbox` / `status` / `test` 四个本地 HTTP 接口。
- 自检用例：假 SMTP 服务器（协议层）、假 cordis 上下文（触发判定）、DOM 桩（浏览器半侧）。
