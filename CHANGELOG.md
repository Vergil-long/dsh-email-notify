# 更新记录

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## 0.5.4

**界面**

- 主界面顶部那段「设置存在 config.json 里、授权码不回显」的说明**挪进高级设置** —— 日常使用不需要看到它。
- 状态框**只留两行**：「状态：配置就绪」和「上次发信」。原来的「当前判定」「发信方式」「配置文件路径」
  都去掉了：判定和路径对使用者没有意义，发信方式看会话页头那颗按钮就够了。
  （这些信息仍然可以从 `GET /dsh-email-notify/status` 和 `scripts/verify-live.mjs` 拿到。）
- **「保存设置」按钮不再带常驻灰底**：早先给它加了 `primary` 样式，看起来像"一直按着没弹起来"，
  和旁边两个按钮明显不一样。现在三个按钮同一套样式，只在鼠标悬停/按下时有反馈。
- 邮件通道的说明改成更明确的说法（并支持换行显示）：
  > 请注意：授权码为 IMAP/SMTP 授权码，不是登录密码
  > QQ 邮箱的授权码在：网页版邮箱 → 设置 → 账户与安全 → 安全设置 → POP3/IMAP/SMTP/Exchange/CardDAV 服务 → 生成授权码

## 0.5.3

**修复**

- **改了样式却不生效**：`ensureCss` 只判断"页面里有没有这个 `<style>`"，有就直接返回 ——
  于是插件升级/客户端热重载后，DOM 按新代码重建了、旧样式却还留在页面里，
  表现就是"文字更新了、样式没变"（0.5.2 加的预览分隔线看不见，就是这个原因）。
  现在内容变了会**替换**旧样式。客户端测试里补了对应的回归用例。
- 预览前那条分隔线改用 `currentColor` + 半透明，深浅主题下都看得见（原先写死的灰色太淡）。

**界面**

- 预览区的标题简化成一个词：**预览**。

## 0.5.2

**界面**

- 「邮件内容」里的**预览区和上面的设置之间加了一条虚线分隔**，并加了一行说明
  「预览 · 这三颗按钮切换三种通知的样子（只改预览，不动你的设置）」。
- 预览的切换按钮（任务完成 / 需要授权 / 等你回答）改成**分段控件**样式（连在一起、选中的填色），
  和上面那些圆角占位符标签一眼就能区分开 —— 此前两者大小一样、又紧挨在下一排，看不出那是预览。
- 预览框加了淡背景，让它看起来就像"一段样例邮件"。

## 0.5.1

**变更**

- 占位符 `{结果}` 改名为 **`{状态}`**（"状态"比"结果"更贴切）。默认模板里的那一行也随之变成 `状态：{状态}`。
- **旧名字继续兼容**：已经存过自定义模板的人，模板里写的 `{结果}` 照样渲染成"已完成 / 出错 / …"，
  也不会被当成拼错的占位符报警。只有设置界面里的占位符清单改列新名字 `{状态}`。

## 0.5.0

**新增**

- 新占位符 **`{离开模式}`**：取值为 `开` / `关` / `自动`（页头那个开关的状态；自动模式下写「自动」）。
  默认正文里就带着一行 `离开模式：{离开模式}`，一眼能看出这封邮件是在什么状态下发出来的。

**变更**

- **默认正文改成**（比 0.3/0.4 更明确，并去掉了原来那串解释性的括号尾注）：

  ```
  工作区：{工作区}
  会话：{会话}
  结果：{结果}
  离开模式：{离开模式}
  {详情}

  这封邮件由 dsh-email-notify 发送
  ```

  `{详情}` 紧随「离开模式」：任务完成时它是空的、**不会多出空行**（渲染出来就是那五行）；
  轮到「需要授权」「等你回答」时才展开成工具名与原因、或问题原文与选项 —— 否则手机上看到
  「结果：等你回答」却不知道问题是什么。

- 界面上「**主题模板**」改名为「**标题模板**」（相应「主题前缀」→「标题前缀」、分组「主题与地址」→「标题与地址」）。
  用「主题」容易和"界面主题"混淆，而这里说的就是邮件标题。
- **标题模板改成单行输入框**：它天生只有一行，之前和正文框一样高，白占半屏。
- 页头按钮开启态的颜色**从绿色改为蓝色**：绿色是"成功/正常"的语义，而这个状态的含意是
  "我不在，把消息转给我"，配绿色不对味。深色主题下另有一档更亮的蓝。
- 删掉高级设置里几处口语化的说明（"这些平时不用改""换别的邮箱服务商时才需要动"等），只留事实与操作。

## 0.4.0

**新增**

- **会话页头的「离开模式」开关**（挂在 `conversation.session.header.utilities`，即页头最右侧）：
  点一下 = **有通知就发邮件**（不管你在不在看界面）；再点一下 = **一封都不发**。状态记在配置里，重启后保持。
  按钮上直接写着当前状态（`离开模式：开` / `离开模式：关`），悬停有说明。

  **为什么要把决定权交回给人**：原先只看 `document.hasFocus()` + `visibilityState`，而"人走了、
  窗口还开着（甚至还是焦点）"恰恰是最常见的情况 —— 那时判定会以为你还在看，于是你在外面一封都收不到。
  自动判定再怎么修也只是猜，所以现在默认由按钮说了算。

**变更（⚠️ 会改变默认行为）**

- **默认不再自动发邮件**：`awayMode` 默认关，也就是**装了新版、没点过按钮之前，一封通知邮件都不会发**。
  想恢复"离开电脑就自动发"的旧行为，到「设置 → 邮件通知 → 高级设置 → 发信时机」打开
  **自动模式**（`autoMode`）—— 打开后页头按钮会变灰并注明原因（它就管不着了）。
- 设置面板顶部的状态行新增**「发信方式」**，一眼看到现在到底会不会发；
  `GET /status` 也新增 `awayMode` / `autoMode` / `willMail` 三个字段；启动日志同样会写明当前处在哪种方式。
- `scripts/verify-live.mjs` 新增一条**「发信总闸（离开模式）」**报告行。

## 0.3.0

**新增**

- **设置面板改成两级**：基础页只需要填「邮箱账号 + 授权码」（收件地址可选），把 SMTP 服务器/端口/加密方式、
  发件显示名、主题前缀、通知时机、限流与启用开关全部收进**高级设置**二级页（点「高级设置 ▸」进入，可返回）。
  默认的 QQ 邮箱参数已经填好，新手不用碰任何一个高级项。
- **邮件内容可以自己改**：主题与正文都变成模板，用 `{会话}` `{工作区}` `{结果}` `{详情}` `{最后回复}`
  这类占位符书写。设置界面里带**实时预览**（任务完成 / 需要授权 / 等你回答三种各看一遍）、
  可点击插入的占位符清单、以及「恢复默认（精简）」「套用详细模板」两个按钮。
- `{详情}` 会在「需要授权」时给出工具名与原因，「等你回答」时给出问题原文与选项 —— 手机上就能决定要不要回。
- 「发送测试邮件」现在**按你当前的模板渲染**，所以点一下就能在手机上看到真通知的样子（不用等真触发一次）。

**修复**

- **邮件里显示的是内部会话 id**（`session-b4d28e9c-…`）而不是左侧列表里那个会话名。现在统一显示侧栏名，
  会话名的来源也加了一层兜底：内存事件 → 会话日志 → **侧栏投影缓存**
  （`<DSH_HOME>/storages/session_projcache.json`）。连标题都没有时才显示"（未命名会话）"。

**变更**

- **默认正文精简成四行**：工作区 / 会话 / 结果 + 一行尾注。此前默认把整段助手回复（最长 700 字）塞进去，
  手机上根本看不完。想看详细内容就把 `{提问}` `{最后回复}` 写进模板，或用「套用详细模板」。
- `includeExcerpts` 这个开关被模板取代（模板里有没有 `{提问}` / `{最后回复}` 就是开关）。
  老配置文件里的这个键不会报错，只是不再起作用。
- 模板留空表示"用内置默认"，所以没自定义过的用户会继续跟随插件升级后的默认模板；
  设置界面里会把默认模板预填给你看/改。

## 0.2.0

**新增**

- **设置集成进 harness**：DSH 的「设置 → 邮件通知」里可以改全部配置（SMTP 服务器/端口/账号/授权码/收件人、
  三种触发开关、界面提示开关、邮件内容、限流参数），带「保存设置」「发送测试邮件」「重新读取」三个按钮，
  还会显示当前状态（配置是否就绪、你此刻是否被判定为"在看界面"、上次发信结果）。
  装好后不再需要命令行或手改 `config.json`。
- **新增「助手向我提问」通知**：工具 `ask_user_question` 被调用（任务停在等你回答）时，离开电脑就发邮件。
  邮件里带上问题原文与选项。
- `GET/POST /dsh-email-notify/config` 接口：读配置不回显授权码（只报"已设置"），写配置时授权码留空表示不修改。
- `POST /dsh-email-notify/test` 支持传入界面上的**草稿**配置，所以"还没保存也能先试发一封"。
- `CHANGELOG.md`、`.gitignore`、仓库元数据（作者/仓库/issue 地址），可直接发布到 npm 或 GitHub。

**修复**

- **邮件主题缺任务标题**：插件通常在会话已经开始之后才挂载（或 DSH 重启过），内存里没有标题事件。
  现在会回读会话日志里的 `session/title`，主题与正文都能带上任务标题。
- **界面提示文案为空**：宿主端推给客户端的字段名（`title`/`outcome`）和客户端渲染用的
  （`notifyTitle`/`notifyBody`）对不上，会导致"看界面时弹提示"弹出空白卡片。现已统一，并由测试守住这个契约。
- 测试邮件的结果会记进 `/status` 的 `lastSend`（此前发过测试邮件后仍显示 `null`，容易让人误以为没发出去）。

**变更**

- `notifyWhenFocused` 默认改为 `false`：桌面外壳自己会在看界面时弹"任务完成"系统通知
  （它的误报已在 `main.js` 里修掉），插件不再重复弹一条。想由插件接管就打开这个开关。
- 配置文件模板改为**空值**（此前写的是"你的QQ邮箱@qq.com"之类的占位文字，显示在设置界面里会被误当成真实值）。
- 安装脚本不再在命令行里索要邮箱与授权码，只负责装插件；配置一律在设置界面里填。
- `smtp.allowInsecure`（允许无 TLS 明文发送）从"隐式默认"变成正式配置项，设置界面里可勾选，默认关闭。
- 设置面板保存时**只回传改动过的字段**（不再把整份配置写回去），这样以后升级插件改默认值仍然生效，
  也不会把你没碰过的默认项固化进 `config.json`。
- 新增 `scripts/update-all.mjs`（双击 `更新并修复.cmd`）：一次做完"同步插件 + 打外壳补丁"，
  顺序固定、可 `--dry` 预演；DSH 还在运行时会安全跳过外壳补丁并提示稍后补跑。
- 新增 `test/manifest.test.mjs` 清单自检：按 `dsh-client-modules` 的实际校验规则核对
  `dsh.bundle.patch` / `dsh.client` / `exports["./client"]` / 浏览器半侧的 `__ModuleLoader__` 包装
  —— 这一层出错是"静默失败"（插件装上了但设置页里没有那一节），必须由用例守住。
- 新增 `test/host-integration.test.mjs` 真运行时自检：用 DSH 自带的 cordis 起真 Context、
  装真的 `dsh-host-webserver`，把宿主半侧挂上去，再走**真 HTTP** 断言（挂载不报错、五个路由的状态码与 JSON、
  口令不回显与留空不覆盖、presence 改变在场判定、405/404，以及 dispose 后路由消失、端口关闭）。
  本机没装 DSH 时自动跳过。
- 新增 `scripts/verify-live.mjs`：重启后一条命令核验插件是否真的加载、浏览器半侧是否装配进页面
  （读 `window.__DSH_BOOT__`）、外壳补丁是否已打，每项不过都给出修复提示。

**修复**

- **外壳补丁脚本的 fail-open 隐患**：`spawnSync('tasklist')` 在权限受限时**不抛异常**，
  只把错误放进返回值、stdout 留空；原来的判断会把它当成"DSH 没在运行"，
  等于在程序开着时去写被占用的 `app.asar`。现在改为**三态查询 + fail-closed**
  （查不到就拒绝写入，除非显式 `--force`），并新增**写完读回核对**（对不上自动用备份还原）。

## 0.1.0

首个版本。

- 宿主端订阅会话事实：`turn/end`（任务完成/出错/中止）、`approval/asked`（工具请求授权）。
- 由浏览器半侧上报窗口焦点与可见性判定"是否在看界面"：在看就不发邮件，离开才发。
- 零依赖 SMTP 客户端：465 隐式 TLS / 587 STARTTLS、AUTH LOGIN·PLAIN、RFC 2047 中文主题、base64 正文，
  失败时给中文可读提示；默认拒绝没有 TLS 的明文服务器以保护授权码。
- 提供 `presence` / `inbox` / `status` / `test` 四个本地 HTTP 接口。
- 自检用例：假 SMTP 服务器（协议层）、假 cordis 上下文（触发判定）、DOM 桩（浏览器半侧）。
